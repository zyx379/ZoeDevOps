前置要求：
1、根据这份文档理解我的需求，完成开发。
2、独立思考，如果有更好的方案，可以替代这份文档的方案。
3、如果描述需求的语义有歧义，不要直接修改，向我确认后再开发。
4、完成开发后尽可能的做好测试。
5、改造内容记录在这份文档中，方便后续查看。

需求：
1、AI报表的对话框支持输入excel、txt、图片进行AI分析处理

实现记录：

1. 附件类型与入口
   - 支持拖拽或点击输入区左侧 📎 按钮选择文件。
   - 格式：`.xlsx/.xls`、`.txt/.csv/.log/.md`、`.png/.jpg/.jpeg/.webp/.gif/.bmp`。
   - 可仅上传附件发送（无文字时自动附带默认分析提示）。

2. 主进程解析 `main/report/attachmentParser.ts`
   - Excel：解析全部 Sheet，表头 + 前 5 行预览写入对话上下文。
   - 文本：UTF-8 读取，超长截断至 32000 字符并标注。
   - 图片：调用 DeepSeek 多模态接口（`chatWithImage`）做 OCR/表格识别，结果写入上下文；若 API/模型不支持 vision，提示改用文字描述或 Excel。

3. IPC
   - 新增 `report:parseAttachment`；保留 `report:parseExcel` 兼容旧调用。

4. 前端 `renderer/utils/reportAttachment.ts` + `Report.tsx`
   - 附件芯片展示类型图标；发送前显示「解析附件…」状态。

5. 测试
   - `npm run test:attachment` — 主进程附件解析
   - `npm run test:report-attachment` — 前端扩展名识别